global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: exsoftoptic-backend
    metrics_path: /metrics
    scheme: http
    bearer_token_file: /run/secrets/exsoftoptic_metrics_token
    static_configs:
      - targets: ["backend:8000"]
